Goal: to understand the effect of optimization on the landscape of trained neural networks

Meaning of "effect of optimization." What should we change?
- We will fix the architecture. Shallow CNN. Define cnn_block = (conv-relu-pool):
    cnn_block-cnn_block-cnn_block-flatten-dense-dense-softmax
- We will fix the overall optimizer: AdamW, which features decay rates for gradient moments + weight decay
- Why PWCCA? Why not direct comparison of weights or activations? A: It is not guaranteed (in fact, not expected)
that two identical NNs (even under the same ordering of training data) will converge to networks with similar
weights. Rather, what we might expect is that the *representations* that the layers are learning are similar.
This can be quantified by using canonical correlations between layers, rather than say, the Frobenius distance.

May 11: so the PWCCA paper (Morcos, Raghu, Bengio 2018) had already considered the effect of learning rate on the
space of converged neural networks. They found that trained neural networks (with varying initialization) clustered
based on their learning rates. This establishes the fact that (1) learning rate affects the final trained neural
network and (2) that the space of trained neural networks satisfies some kind of locality: similarly-trained neural
networks are "closer" in representation space.

Therefore, as a follow-up question: what is the effective dimension of this space? I.e. we have the Hyperparameters
(1) batch_norm / learning rate (scaled linearly) (2) weight_decay (3) beta1 (4) beta4. But my guess is that the actual
space of neural networks has lower effective dimension.

Natural follow-up: what is the theoretical explanation for this fact?
Natural follow-up II: how does the effective dimension change with time? with layer?

May 12: Should implement CKA, which is a similarity measure which can handle not only activations of identical layers,
but also activations of differently-shaped layers across different neural networks.

Replace neural network with autoencoder

May 19: We use a VGG-style downsampling convolutional encoder which gradually downsamples rather than all at once. This
type of architecture typically leads to hierarchical features, which will be interesting to analyze.
Hypothesis: autoencoders trained with higher learning rates will lead to more aggressive feature sampling in earlier
layers

May 22: We train three populations of neural networks, optimized differently
    1. Adam_atan2: lr=0.001, weight_decay=1e-2, batch_size=32, num_epochs=100
    2. AdamW: lr=0.001, weight_decay=1e-2, batch_size=32, num_epochs=100
    3. SGD: lr=0.005, weight_decay=5e-4, momentum=0.9, batch_size=256, num_epochs=200
        (taken from https://proceedings.mlr.press/v202/refinetti23a/refinetti23a.pdf
        "Neural networks trained with SGD learn distributions of increasing complexity")

Notes:
- Data split: train, val, test
    - Test set will be used to compute NN representation vectors
- No dropout layers (adds randomness during training procedure, which we want to avoid).
- Instead of AdamW, use the Adam-atan2 optimizer (https://arxiv.org/pdf/2407.05872). This is "a new numerically stable,
    scale-invariant version of Adam that eliminates the epsilon hyperparameter entirely"

Data:
- CIFAR-10. Can be learned to around 75% accuracy with shallow CNN.

- Hyperparameters to tune
    - Obvious: learning rate. should have a big effect on trained classifier
    - Less obvious: fixing lr and weight_decay, vary \beta_1, \beta_2
    - What is the space of trained networks?
    - Think more about which hypers to vary between experiments

To do:
- Implement save_every: we want to save the state of the NN over the course of the training process
- Understand Hydra and create config files for experiments
- Build optimizer and scheduler

Data we have:
- Training data: train/val loss as function of time
- Similarity between any two neural networks (N_layer x N_layer matrices M)
    Can produce distance measure:
    - Distance between layers = 1 - similarity
    Can produce scalar/aggregate/global similarity measures:
    - Mean Diagonal Similarity: S = (1/L) \sum_{i=1}^L M_{ii}
    - Banded Mean Diagonal Similarity: S_{banded} = (1/L) \sum_{i=1}^L \max_{j\in i-1, i, i+1} M_{ij}, can generalize
        to thicker bands
    - Trace to sum ratio: r = Tr M / \sum_{i,j} M_{ij}

Plots to make:
- Pairwise NN similarity (among sgd, adamw, adam_atan2) of last layer of each encoder block versus training epoch, for pwcca and cka
    * to see how similarity changes as training progresses
- How does overall similarity, as measured by the aggregate metrics above, change with training time?
- Low-dimensional visualization of neural network similarity based on their aggregate similarity score (MDS, t-SNE)
    * clustering?
- Self-similarity of the same layer across different points in time. Superposed with CosineAnnealed learning rate.